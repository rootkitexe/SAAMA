const { createClient } = require('@supabase/supabase-js'); require('dotenv').config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const { error } = await supabase.from('upcoming_events').update({ image: '/aaroha-highlight.jpg' }).eq('title', 'Aaroha Carnatic Music Festival'); console.log('Updated with error:', error); } run();
